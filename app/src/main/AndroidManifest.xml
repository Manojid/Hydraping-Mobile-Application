<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".HydroPingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@null"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HydroPing">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HydroPing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget Receiver -->
        <receiver
            android:name=".widget.HydroWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hydro_companion_widget_info" />
        </receiver>

        <!-- Reminder Alarms Receiver -->
        <receiver
            android:name=".reminder.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hydroping.app.ACTION_TRIGGER_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Quick Drink Broadcast Receiver (Notification Actions) -->
        <receiver
            android:name=".reminder.QuickDrinkReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hydroping.app.ACTION_QUICK_DRINK" />
                <action android:name="com.hydroping.app.ACTION_DRINKING_NOW" />
                <action android:name="com.hydroping.app.ACTION_REMIND_LATER" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver for Alarm Rescheduling -->
        <receiver
            android:name=".reminder.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings Tile Service -->
        <service
            android:name=".service.HydroQuickTileService"
            android:exported="true"
            android:icon="@drawable/pikachu_happy"
            android:label="@string/qs_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>

</manifest>
